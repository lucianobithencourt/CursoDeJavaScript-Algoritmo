


// switch-case

let element = prompt (`Selecione uma das opções desejadas :  
[01] - CALCULAR IMC -
[02] - CALCULAR SUA IDADE -
[03] - CONVERTER TEMPERATURAS -  
`, `Digite aqui`)

let opcao = Number(element)

switch (opcao) {
    case 1:
        alert('opcao 1 - seleconada')
        
        break;

        case 2:
        alert('opcao 2 - seleconada')
        break;

        case 3:
        alert('opcao 3 - seleconada')
        break;

    default:
        break;
}